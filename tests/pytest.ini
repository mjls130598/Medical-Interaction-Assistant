[pytest]
# Etiquetas para filtrar tests
markers =
    unit: Pruebas unitarias rápidas y aisladas.
    integration: Pruebas que requieren bases de datos o servicios externos.

# Opciones por defecto
testpaths = tests
python_files = test_*.py
addopts = -v --strict-markers